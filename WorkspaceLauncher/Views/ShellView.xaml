<Window x:Class="WorkspaceLauncher.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkspaceLauncher.Views"
        mc:Ignorable="d"
        Title="Workspace Launcher" Height="250" Width="400">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="20"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Menu Grid.Row="0" Grid.ColumnSpan="2">
			<MenuItem Header="File">
				<MenuItem Header="Add Profile" x:Name="AddProfile"/>
				<MenuItem Header="Rename Profile" x:Name="RenameProfile"/>
				<MenuItem Header="Delete Profile" x:Name="DeleteProfile" Command="{Binding DeleteProfileCommand}"/>
				<MenuItem Header="Settings" x:Name="Settings" Command="{Binding SettingsCommand}"/>
			</MenuItem>
		</Menu>
		<StackPanel Grid.Row="1">
			<TextBlock Text="Profiles"/>
			<ListBox x:Name="ProfileList" Height="175" ItemsSource="{Binding Profiles}" DisplayMemberPath="ProfileName" SelectedItem="{Binding SelectedProfile}"/>
		</StackPanel>
		<StackPanel Grid.Row="1" Grid.Column="1">
			<Button x:Name="SetPrograms" Content="Set Programs" Command="{Binding SetProgramsCommand}"/>
			<Button x:Name="Launch" Content="Launch" Command="{Binding LaunchCommand}"/>
			<Button x:Name="Move" Content="Move" Command="{Binding MoveCommand}"/>
			<Button x:Name="LaunchAndMove" Content="Launch and Move" Command="{Binding LaunchMoveCommand}"/>
		</StackPanel>
	</Grid>
</Window>
