<Window x:Class="WorkspaceLauncher.Views.SelectProcessesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkspaceLauncher.Views"
        mc:Ignorable="d"
        Title="SelectProcessesView" Height="400" Width="500" ResizeMode="NoResize">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<StackPanel>
			<Button Content="Refresh" Command="{Binding RefreshOpenWindowsCommand}"/>
			<TextBlock Text="Open Windows"/>
			<ListBox x:Name="OpenProcesses" ItemsSource="{Binding OpenWindows}" DisplayMemberPath="ProcessName" Height="300" SelectionMode="Multiple" SelectionChanged="OpenProcesses_SelectionChanged"/>
			<Button Content="Save state of selected process" Command="{Binding SaveProcessesCommand}"/>
		</StackPanel>
		<StackPanel Grid.Column="1">
			<Button Content="Clear saved"  Command="{Binding ClearProcessesCommand}"/>
			<TextBlock Text="Currently Saved"/>
			<ListBox ItemsSource="{Binding ProfilePrograms}" DisplayMemberPath="ProcessName" Height="300"/>
		</StackPanel>
    </Grid>
</Window>
