<Window x:Class="WorkspaceLauncher.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkspaceLauncher.Views"
        mc:Ignorable="d"
        Title="Settings" Height="600" Width="400" ResizeMode="NoResize" Background="#d4d4d4">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="125"/>
			<RowDefinition Height="225"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<StackPanel Grid.ColumnSpan="2">
			<StackPanel Orientation="Horizontal" Margin="10">
				<TextBlock Text="Version: "/>
				<TextBlock Text="{Binding Version}" Width="100"/>
				<Button Content="Check for updates" Command="{Binding CheckForUpdatesCommand}"/>
			</StackPanel>
			<StackPanel Orientation="Horizontal">
				<CheckBox Content="Check for updates" IsChecked="{Binding CheckForUpdates}" Margin="10,5"/>
				<CheckBox Content="Always on top" IsChecked="{Binding AlwaysOnTop}" Margin="10,5"/>
			</StackPanel>
			<StackPanel>
				<TextBlock Text="Profile to load on start" Margin="10,10,0,0"/>
				<ComboBox SelectedItem="{Binding LaunchProfile}" ItemsSource="{Binding ProfilesList}" Width="200" HorizontalAlignment="Left" Margin="10,0,0,0"/>
			</StackPanel>
		</StackPanel>
		<StackPanel Grid.Row="1">
			<TextBlock Text="Profiles" Margin="10,10,0,0"/>
			<ListBox Height="185" ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" Margin="10,0">
				<ListBox.ContextMenu>
					<ContextMenu>
						<MenuItem Header="Delete Profile" Command="{Binding DeleteProfileCommand}"/>
						<MenuItem Header="Rename Profile" Command="{Binding RenameProfileCommand}"/>
						<MenuItem Header="Add Programs" Command="{Binding AddProgramsCommand}"/>
					</ContextMenu>
				</ListBox.ContextMenu>
			</ListBox>
		</StackPanel>
		<StackPanel Grid.Row="1" Grid.Column="1">
			<TextBlock Text="Programs" Margin="10,10,0,0"/>
			<ListBox Height="185" ItemsSource="{Binding Programs}" DisplayMemberPath="ProcessName" SelectedItem="{Binding SelectedProgram}" Margin="10,0">
				<ListBox.ContextMenu>
					<ContextMenu>
						<MenuItem Header="Remove program" Command="{Binding RemoveProgramCommand}"/>
					</ContextMenu>
				</ListBox.ContextMenu>
			</ListBox>
		</StackPanel>
		<StackPanel Grid.Row = "3" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,10">
			<StackPanel>
				<TextBlock Text="Window State"/>
				<ComboBox Width="100" HorizontalAlignment="Left" ItemsSource="{Binding WindowStateList}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding SelectedProgram.WindowState}" IsEnabled="{Binding ProgramSelected}"/>
			</StackPanel>
			<StackPanel Orientation="Horizontal">
				<StackPanel>
					<TextBlock Text="Window Width" Width="85"/>
					<TextBox Width="75" Text="{Binding SelectedProgram.WindowWidth}" IsEnabled="{Binding ProgramSelected}"/>
					<TextBlock Text="X Position" Width="55"/>
					<TextBox Width="75" Text="{Binding SelectedProgram.XPos}" IsEnabled="{Binding ProgramSelected}"/>
				</StackPanel>
				<StackPanel Margin="5,0">
					<TextBlock Text="Window Height" Width="85"/>
					<TextBox Width="75" Text="{Binding SelectedProgram.WindowHeight}" IsEnabled="{Binding ProgramSelected}"/>
					<TextBlock Text="Y Position" Width="55"/>
					<TextBox Width="75" Text="{Binding SelectedProgram.YPos}" IsEnabled="{Binding ProgramSelected}"/>
				</StackPanel>
			</StackPanel>
			
			<StackPanel Margin="5,0">
				<TextBlock Text="Launch Path"/>
				<StackPanel Orientation="Horizontal">
					<TextBox Width="300" Text="{Binding SelectedProgram.StartPath}" IsEnabled="{Binding ProgramSelected}"/>
					<Button Content="Find File" VerticalAlignment="Top" Margin="10,0" Command="{Binding FindFileCommand}" IsEnabled="{Binding ProgramSelected}"/>
				</StackPanel>
				<TextBlock Text="Start Arguments"/>
				<TextBox Width="300" Text="{Binding SelectedProgram.Argument}" HorizontalAlignment="Left" IsEnabled="{Binding ProgramSelected}"/>
			</StackPanel>
		</StackPanel>
	</Grid>
</Window>
